<Page x:Class="SEDA.Wpf.Pages.RepeatedTasksPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="RepeatedTasksPage">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Repeated Tasks" FontSize="22" FontWeight="SemiBold"/>
            <TextBlock Text="Find workflows you repeat and inspect a representative run." Style="{StaticResource MutedText}" Margin="0,6,0,0"/>
        </StackPanel>

            <DockPanel Grid.Row="1" Margin="0,18,0,0" LastChildFill="False">
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                <TextBlock Text="Min repeats" Style="{StaticResource MutedText}" VerticalAlignment="Center"/>
                <TextBox Text="{Binding MinRepeats}" Width="60" Margin="10,0,18,0"/>
                <TextBlock Text="Limit" Style="{StaticResource MutedText}" VerticalAlignment="Center"/>
                <TextBox Text="{Binding Limit}" Width="60" Margin="10,0,18,0"/>
                <TextBlock Text="Search" Style="{StaticResource MutedText}" VerticalAlignment="Center"/>
                <TextBox Text="{Binding Query}" Width="240" Margin="10,0,0,0"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                <Button Click="Load_OnClick" Style="{StaticResource PrimaryButton}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE72C;" Margin="0,0,8,0"/>
                        <TextBlock Text="Load"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </DockPanel>

        <Grid Grid.Row="2" Margin="0,18,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <DockPanel Grid.Column="0" Margin="0,0,12,0">
                <TextBlock DockPanel.Dock="Top" Text="Bundles" FontWeight="SemiBold" Margin="0,0,0,8"/>
                <ListBox ItemsSource="{Binding Bundles}" SelectionChanged="Bundles_OnSelectionChanged">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <TextBlock Text="{Binding Title}" FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding Subtitle}" Style="{StaticResource MutedText}" TextTrimming="CharacterEllipsis" MaxHeight="34"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>

            <Border Grid.Column="1" Background="{StaticResource AppBg}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="12" Padding="14">
                <StackPanel>
                    <TextBlock Text="Preview" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding PreviewLine}" Style="{StaticResource MutedText}" Margin="0,6,0,0"/>
                    <TextBlock Text="{Binding ErrorLine}" Foreground="#FFFCA5A5" Margin="0,12,0,0"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Page>

